<UserControl x:Class="ExcelExport.Views.PropertiesFilesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ExcelExport.Views"
             xmlns:ViewModels="clr-namespace:ExcelExport.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type ViewModels:PropertiesFileListViewModel}}"
             Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <ListView x:Name="ListView_PropertyFiles"
                  ItemsSource="{Binding SelectedPropertyFiles}" 
                  Grid.Row="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding FilePath}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <Button x:Name="Btn_AddFile"
                Content="Add File"
                Command="{Binding AddFileCommand}" 
                Grid.Row="1"/>
        
    </Grid>
</UserControl>